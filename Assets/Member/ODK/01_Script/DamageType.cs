using UnityEngine;

namespace Member.ODK.Scripts
{
    public enum DamageType
    {
        Melee,
        Beam,
        Projectile,
        Special
    }

    public enum CriticalType
    {
        Normal,
        Critical,
        SuperCritical
    }
}
